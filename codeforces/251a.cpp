#include <bits/stdc++.h>
using namespace std;
typedef long long num;

const int MAX = 1e5+1;

int main(){
	int n, d, i, k;
	num x[MAX];

	scanf("%d%d", &n, &d);
	for(i = 0; i < n; i++) cin >> x[i];

}
